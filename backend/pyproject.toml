[project]
name = "shuttle-booking-backend"
version = "0.1.0"
description = "Airport shuttle booking backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
    "sqlalchemy",
    "alembic",
    "httpx",
    "resend",
    "pydantic-settings",
    "psycopg2-binary",
]
